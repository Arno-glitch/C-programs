//This is a program on area of a rectangle 
#include <stdio.h>

int main() {
  printf("Area of a rectangle\n");
    int x,y,area;
printf("input the length");
scanf("%d",&x);
printf("input the width");
scanf("%d",&y);
area=x*y;
printf("area=%d",area);
    return 0;
}
